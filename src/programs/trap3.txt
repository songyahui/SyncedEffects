(signal A (signal B (signal C (signal D (signal E (signal F (signal G 

(
(trap 
  (
  (emit A;
    (pause;
      (emit B;
        ((exit 0);
          emit C
        )
      )
    )
  )
  ||
  (emit E;
    (pause;
      ((exit 1);
        (pause;
          emit G
        )
      )
    )
  )
)
);
emit D
)



))))
)))
