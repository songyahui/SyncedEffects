(signal A (signal B (signal C (signal D (signal E (signal F (signal G 

(
(trap 
  (
  (emit A;
    (pause;
      (emit B;
        ((exit 0);
          emit C
        )
      )
    )
  )
  ||
  (emit E;
    (pause;
      (emit F;
        (pause;
          emit G
        )
      )
    )
  )
)
);
emit D
)



))))
)))
